type User {
	userID: Int!,
	username: String!,
	key: String!
}

type Query {
	readUser(token: String!, username: String!): User
}

type Mutation {
	createUser(username: String!, password: String!, key: String!): String
	updateUser(token: String!, username: String!, password: String!, key: String!): String
}